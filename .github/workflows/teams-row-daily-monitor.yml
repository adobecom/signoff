name: Teams ROW Daily Monitor

on:
  schedule:
    - cron: '0 2 * * *'
    - cron: '0 3 * * *'
    - cron: '0 4 * * *'
    - cron: '0 5 * * *'
    - cron: '0 6 * * *'
    - cron: '0 7 * * *'
    - cron: '0 8 * * *'
    - cron: '0 9 * * *'
    - cron: '0 14 * * *'
    - cron: '0 15 * * *'
    - cron: '0 16 * * *'
    - cron: '0 17 * * *'
    - cron: '0 18 * * *'
    - cron: '0 19 * * *'
    - cron: '0 20 * * *'
    - cron: '0 21 * * *'
  workflow_dispatch:
    inputs:
      test_url:
        description: 'Test URL (overrides locale selection)'
        required: false
        type: string
      locale:
        description: 'Locale to test'
        required: false
        type: choice
        options:
          - 'all'
          - 'ae_ar'
          - 'ae_en'
          - 'africa'
          - 'ar'
          - 'at'
          - 'au'
          - 'be_en'
          - 'be_fr'
          - 'be_nl'
          - 'bg'
          - 'br'
          - 'ca'
          - 'ca_fr'
          - 'ch_de'
          - 'ch_fr'
          - 'ch_it'
          - 'cl'
          - 'cn'
          - 'co'
          - 'cr'
          - 'cz'
          - 'de'
          - 'dk'
          - 'ec'
          - 'ee'
          - 'eg_ar'
          - 'eg_en'
          - 'es'
          - 'fi'
          - 'fr'
          - 'gr_el'
          - 'gr_en'
          - 'gt'
          - 'hk_en'
          - 'hk_zh'
          - 'hu'
          - 'id_en'
          - 'id_id'
          - 'ie'
          - 'il_en'
          - 'il_he'
          - 'in'
          - 'in_hi'
          - 'it'
          - 'jp'
          - 'kr'
          - 'kw_ar'
          - 'kw_en'
          - 'la'
          - 'lt'
          - 'lu_de'
          - 'lu_en'
          - 'lu_fr'
          - 'lv'
          - 'mena_ar'
          - 'mena_en'
          - 'mx'
          - 'my_en'
          - 'my_ms'
          - 'ng'
          - 'nl'
          - 'no'
          - 'nz'
          - 'pe'
          - 'ph_en'
          - 'ph_fil'
          - 'pl'
          - 'pr'
          - 'pt'
          - 'qa_ar'
          - 'qa_en'
          - 'ro'
          - 'ru'
          - 'sa_ar'
          - 'sa_en'
          - 'se'
          - 'sg'
          - 'si'
          - 'sk'
          - 'th_en'
          - 'th_th'
          - 'tr'
          - 'tw'
          - 'ua'
          - 'uk'
          - 'vn_en'
          - 'vn_vi'
          - 'za'
      test_tabs:
        description: 'Test Tabs'
        required: false
        type: string
      slack_channel_passed:
        description: 'Slack channel for passed tests'
        required: false
        type: string
        default: 'loot-loaders-tests'
      slack_channel_failed:
        description: 'Slack channel for failed tests'
        required: false
        type: string
        default: 'loot-loaders-tests'

jobs:
  filter-matrix:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        run: |
          CURRENT_HOUR=$(date -u +%H)

          if [ "${{ github.event_name }}" == "schedule" ]; then
            # Run only the locale matching the current hour
            case "$CURRENT_HOUR" in
              "02"|"14") echo 'matrix={"include":[{"url":"https://www.adobe.com/uk/creativecloud/business/teams.html","locale":"UK"}]}' >> $GITHUB_OUTPUT ;;
              "03"|"15") echo 'matrix={"include":[{"url":"https://www.adobe.com/jp/creativecloud/business/teams.html","locale":"Japan"}]}' >> $GITHUB_OUTPUT ;;
              "04"|"16") echo 'matrix={"include":[{"url":"https://www.adobe.com/de/creativecloud/business/teams.html","locale":"Germany"}]}' >> $GITHUB_OUTPUT ;;
              "05"|"17") echo 'matrix={"include":[{"url":"https://www.adobe.com/fr/creativecloud/business/teams.html","locale":"France"}]}' >> $GITHUB_OUTPUT ;;
              "06"|"18") echo 'matrix={"include":[{"url":"https://www.adobe.com/es/creativecloud/business/teams.html","locale":"Spain"}]}' >> $GITHUB_OUTPUT ;;
              "07"|"19") echo 'matrix={"include":[{"url":"https://www.adobe.com/it/creativecloud/business/teams.html","locale":"Italy"}]}' >> $GITHUB_OUTPUT ;;
              "08"|"20") echo 'matrix={"include":[{"url":"https://www.adobe.com/in/creativecloud/business/teams.html","locale":"India"}]}' >> $GITHUB_OUTPUT ;;
              "09"|"21") echo 'matrix={"include":[{"url":"https://www.adobe.com/br/creativecloud/business/teams.html","locale":"Brazil"}]}' >> $GITHUB_OUTPUT ;;
            esac
          elif [ "${{ inputs.locale }}" == "all" ]; then
            # Manual trigger with no locale: run all
            echo 'matrix={"include":[{"url":"https://www.adobe.com/uk/creativecloud/business/teams.html","locale":"UK"},{"url":"https://www.adobe.com/jp/creativecloud/business/teams.html","locale":"Japan"},{"url":"https://www.adobe.com/de/creativecloud/business/teams.html","locale":"Germany"},{"url":"https://www.adobe.com/fr/creativecloud/business/teams.html","locale":"France"},{"url":"https://www.adobe.com/es/creativecloud/business/teams.html","locale":"Spain"},{"url":"https://www.adobe.com/it/creativecloud/business/teams.html","locale":"Italy"},{"url":"https://www.adobe.com/in/creativecloud/business/teams.html","locale":"India"},{"url":"https://www.adobe.com/br/creativecloud/business/teams.html","locale":"Brazil"}]}' >> $GITHUB_OUTPUT
          else
            # Manual trigger with specific locale
            LOCALE="${{ inputs.locale }}"
            echo "matrix={\"include\":[{\"url\":\"https://www.adobe.com/${LOCALE}/creativecloud/business/teams.html\",\"locale\":\"${LOCALE}\"}]}" >> $GITHUB_OUTPUT
          fi

  monitor:
    needs: filter-matrix
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.56.0-noble
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.filter-matrix.outputs.matrix) }}
    env:
      TEST_URL: ${{ github.event.inputs.test_url || matrix.url }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-

      - name: Install dependencies
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 10
          max_attempts: 3
          retry_wait_seconds: 10
          command: npm ci

      - name: Run teams ROW monitor
        run: npx playwright test teams/monitor_row.spec.js --project=chromium
        env:
          TEST_TABS: ${{ inputs.test_tabs }}
          USER_AGENT_SUFFIX: "${{ secrets.USER_AGENT_SUFFIX }}"

      - name: Process error reports
        if: failure()
        id: process-errors
        run: |
          # Install jq if not available
          if ! command -v jq &> /dev/null; then
            apt-get update && apt-get install -y jq
          fi
          
          ERROR_SUMMARY=""
          if [ -d "test-results" ] && [ -n "$(ls -A test-results/error-report-*.json 2>/dev/null)" ]; then
            echo "Found error report files"
            for file in test-results/error-report-*.json; do
              if [ -f "$file" ]; then
                echo "Processing $file"
                TEST_NAME=$(jq -r '.testName' "$file")
                ERROR_COUNT=$(jq -r '.errorCount' "$file")
                ERRORS=$(jq -r '.errors | join("\n• ")' "$file")
                
                ERROR_SUMMARY="${ERROR_SUMMARY}
          
          **${TEST_NAME}** (${ERROR_COUNT} error(s)):
          • ${ERRORS}
          "
              fi
            done
          else
            ERROR_SUMMARY="No detailed error information available. Check the logs for details."
          fi
          
          # Save to GitHub output using heredoc to handle multiline
          echo "ERROR_SUMMARY<<EOF" >> $GITHUB_OUTPUT
          echo "$ERROR_SUMMARY" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: teams-row-monitor-${{ matrix.locale }}-${{ github.run_number }}
          path: |
            playwright-report/
            screenshots/
            test-results/
          retention-days: 30

      - name: Slack notification on success
        if: success()
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_CHANNEL: ${{ inputs.slack_channel_passed || 'loot-loaders-tests' }}
          SLACK_COLOR: good
          SLACK_ICON: https://github.com/actions.png?size=48
          SLACK_USERNAME: Teams ROW Monitor
          SLACK_MSG_AUTHOR: ""
          MSG_MINIMAL: true
          SLACK_TITLE: ✅ Teams ROW Monitor (${{ matrix.locale }}) - SUCCESS
          SLACK_MESSAGE: |
            Test URL: ${{ env.TEST_URL }}
            View details: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          SLACK_FOOTER: ""
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}

      - name: Slack notification on failure
        if: failure()
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_CHANNEL: ${{ github.event_name == 'schedule' && 'acom-commerce-alerts' || inputs.slack_channel_failed || 'loot-loaders-tests' }}
          SLACK_COLOR: danger
          SLACK_ICON: https://github.com/actions.png?size=48
          SLACK_USERNAME: Teams ROW Monitor
          SLACK_MSG_AUTHOR: ""
          MSG_MINIMAL: true
          SLACK_MESSAGE: |
            Test URL: ${{ env.TEST_URL }}
            
            ${{ steps.process-errors.outputs.ERROR_SUMMARY }}
            
            View details: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          SLACK_TITLE: ❌ Teams ROW Monitor (${{ matrix.locale }}) - FAILURE
          SLACK_FOOTER: ""
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}

